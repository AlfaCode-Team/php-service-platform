<?php

declare(strict_types=1);

/**
 * {{STUDLY}} plugin configuration.
 *
 * Published to the project's config/{{LOWER}}.php when the plugin is enabled
 * (hkm plugins enable {{STUDLY}}). Read values through your config loader /
 * the env() helper. Keep secrets in .env, not here.
 */
return [
    'enabled' => true,

    // Add {{LOWER}} settings here.
    // 'cache_ttl' => (int) (env('{{UPPER}}_CACHE_TTL') ?: 3600),
];
